{
  "name": "agent-helper",
  "version": "1.0.0",
  "description": "Agent Helper - A command-line tool for agents to manage Shopify store data",
  "main": "scripts/agent-tool.js",
  "scripts": {
    "start": "node scripts/agent-tool.js",
    "agent": "node scripts/agent-tool.js",
    "server": "node simple-oauth-server.js",
    "dev": "shopify app dev",
    "deploy": "shopify app deploy",
    "info": "shopify app info",
    "setup": "cp .env.example .env && echo 'Please edit .env with your Shopify credentials'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "shopify",
    "admin-api",
    "cli",
    "agent-tools",
    "store-management",
    "public-app"
  ],
  "author": "Noteware Digital Solutions",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@shopify/shopify-api": "^12.0.0",
    "dotenv": "^17.2.3",
    "express": "^5.1.0"
  }
}
